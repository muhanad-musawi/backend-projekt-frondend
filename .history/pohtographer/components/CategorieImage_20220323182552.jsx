import React from 'react'

function CategorieImage() {

  const 

  return (
    <div>
      {images.map((image, index) => (
        <div key={index}>
          <img
        </div>
      ))}
    </div>
  )
}

export default CategorieImage